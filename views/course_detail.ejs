<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="utf-8">
    <title>
        <%= data.name %> - 詳細
    </title>
    <style>
        body {
            font-family: sans-serif;
            margin: 2rem;
            background: #fcfcfc;
        }

        .card {
            background: white;
            padding: 2rem;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            max-width: 600px;
        }

        h1 {
            margin-top: 0;
            color: #2c3e50;
            border-bottom: 2px solid #eee;
            padding-bottom: 0.5rem;
        }

        .row {
            display: flex;
            border-bottom: 1px solid #eee;
            padding: 0.8rem 0;
        }

        .label {
            font-weight: bold;
            width: 120px;
            color: #7f8c8d;
        }

        .value {
            flex: 1;
            color: #2c3e50;
        }

        .actions {
            margin-top: 2rem;
            display: flex;
            gap: 1rem;
        }

        .btn {
            padding: 0.6rem 1.2rem;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
            color: white;
            font-size: 0.95rem;
        }

        .btn-edit {
            background: #f39c12;
        }

        .btn-delete {
            background: #c0392b;
        }

        .btn-back {
            background: #95a5a6;
        }
    </style>
</head>

<body>
    <div class="card">
        <h1>
            <%= data.name %>
        </h1>

        <div class="row">
            <div class="label">ID</div>
            <div class="value">
                <%= data.id %>
            </div>
        </div>
        <div class="row">
            <div class="label">担当教員</div>
            <div class="value">
                <%= data.teacher %>
            </div>
        </div>
        <div class="row">
            <div class="label">開講期</div>
            <div class="value">
                <%= data.term %>
            </div>
        </div>
        <div class="row">
            <div class="label">時限</div>
            <div class="value">
                <%= data.period %>
            </div>
        </div>
        <div class="row">
            <div class="label">単位数</div>
            <div class="value">
                <%= data.credits %>
            </div>
        </div>
        <div class="row">
            <div class="label">区分</div>
            <div class="value">
                <%= data.type %>
            </div>
        </div>

        <div class="actions">
            <a href="/<%= data.id %>/edit" class="btn btn-edit">編集</a>
            <form action="/<%= data.id %>/delete" method="POST">
                <button type="submit" class="btn btn-delete" onclick="return confirm('本当に削除しますか？');">削除</button>
            </form>
            <a href="/" class="btn btn-back">一覧へ戻る</a>
        </div>
    </div>
</body>

</html>
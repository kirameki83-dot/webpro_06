<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="utf-8">
    <title>IIDX Score Manager</title>
    <style>
        body {
            font-family: sans-serif;
            margin: 2rem;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1rem;
        }

        th,
        td {
            border: 1px solid #ccc;
            padding: 0.5rem;
            text-align: left;
        }

        th {
            background: #f0f0f0;
        }

        .btn {
            padding: 0.5rem 1rem;
            text-decoration: none;
            background: #007bff;
            color: white;
            border-radius: 4px;
        }

        .btn:hover {
            background: #0056b3;
        }

        .lamp-HARD {
            color: red;
            font-weight: bold;
        }

        .lamp-CLEAR {
            color: blue;
        }

        .lamp-FAILED {
            color: gray;
        }
    </style>
</head>

<body>
    <h1>IIDX Score Manager</h1>
    <a href="/create" class="btn">新規登録</a>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>タイトル</th>
                <th>アーティスト</th>
                <th>難易度</th>
                <th>レベル</th>
                <th>スコア</th>
                <th>ランプ</th>
                <th>詳細</th>
            </tr>
        </thead>
        <tbody>
            <% data.forEach(item=> { %>
                <tr>
                    <td>
                        <%= item.id %>
                    </td>
                    <td>
                        <%= item.title %>
                    </td>
                    <td>
                        <%= item.artist %>
                    </td>
                    <td>
                        <%= item.difficulty %>
                    </td>
                    <td>
                        <%= item.level %>
                    </td>
                    <td>
                        <%= item.score %> (<%= item.djLevel %>)
                    </td>
                    <td class="lamp-<%= item.lamp.replace(' ', '-') %>">
                        <%= item.lamp %>
                    </td>
                    <td><a href="/<%= item.id %>">詳細</a></td>
                </tr>
                <% }); %>
        </tbody>
    </table>
</body>

</html>